import React from 'react'
import PropTypes from 'prop-types'

const Client = props => {
    return (
        <div>
            this is client
        </div>
    )
}

Client.propTypes = {

}

export default Client
