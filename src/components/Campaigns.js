import React from 'react'
import PropTypes from 'prop-types'
import Navbar from './Navbar'

const Campaigns = (props) => {
  return <div style={{ marginTop: '30px' }}>this is a campaign</div>

  Campaigns.propTypes = {}
}
export default Campaigns
